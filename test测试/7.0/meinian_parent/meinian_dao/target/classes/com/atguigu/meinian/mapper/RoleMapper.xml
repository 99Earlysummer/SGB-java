<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.meinian.mapper.RoleMapper">
<!--
    <resultMap id="roleMap" type="Role">
        <id column="id" property="id"></id>
        <result column="name" property="name"></result>
        <result column="keyword" property="keyword"></result>
        <result column="description" property="description"></result>
        <collection property="permissions" column="id"
                    select="com.atguigu.meinian.mapper.PermissionMapper.getPermissionByRoleId "></collection>
    </resultMap>
    <select id="findRolesByUserId" parameterType="int" resultMap="roleMap">
        SELECT r.*
        FROM t_role r,
             t_user_role ur
        WHERE r.id = ur.role_id
          AND ur.user_id = #{userId}
    </select>
-->


    <resultMap id="roleResultMap" type="Role">
        <id property="id" column="id"></id>
        <result property="name" column="name"></result>
        <result property="keyword" column="keyword"></result>
        <result property="description" column="description"></result>
        <collection property="permissions"
                    select="com.atguigu.meinian.mapper.PermissionMapper.getPermissionByRoleId"
                    column="id"></collection>
    </resultMap>
    <!--根据用户id查询所对应的角色信息-->
    <select id="getRoleByUserId" resultMap="roleResultMap">
        SELECT tr.* FROM t_user_role tur LEFT JOIN t_role tr ON tur.role_id = tr.id WHERE tur.user_id = #{userId}
    </select>

</mapper>