<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.meinian.mapper.AddressMapper">
    <!--添加地址void addAddress(Address address);-->
    <insert id="addAddress">
        insert into t_address
        values (
                     null,
                     #{addressName},
                     #{lng},
                     #{lat}
                );
    </insert>
    <!--删除方法-->
    <delete id="deleteById">
        delete
        from t_address
        where id=#{id}
    </delete>
    <!--List<Address> findAllMaps();-->
    <select id="findAllMaps" resultType="Address">
        select * from t_address
    </select>
    <!--分页查询-->
    <select id="findPages" resultType="com.atguigu.meinian.pojo.Address">
        select *
        from t_address
        <where>
            <if test="queryString != '' and queryString != null">
                name like "%"#{queryString}"%"
            </if>
        </where>
    </select>



</mapper>